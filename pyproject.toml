[project]
name = "pulumi-docker-plugin-example"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pulumi>=3.159.0",
    "pulumi-random>=4.18.0",
]
